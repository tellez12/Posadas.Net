
    @using (Html.BeginForm("Busqueda", "Posadas", FormMethod.Post, htmlAttributes: new { @class = "navbar-form" }))
    {
        <div class="input-group">
            @Html.TextBox("Query", null, htmlAttributes: new { @class = "form-control", @placeholder="Busqueda" } )
            @*<div class="input-group-btn">*@
            <div class="input-group-btn">
                <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
            </div>
        </div>

    }